default: -p ci-regression-dev-int

ci-smoke-dev-int: --tags @smoke ~@manual SERVER=dev_int --strict
ci-regression-dev-int: --tags ~@in-progress ~@manual SERVER=dev_int --strict

dev: --tags ~@in-progress ~@manual SERVER=local --strict --format pretty
